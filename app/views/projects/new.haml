= render partial: 'nav'

#create-project.contentpane
  = form_for @project do |f|
    .field
      = f.label :name
      = f.text_field :name
    .field
      = f.label :description
      = f.text_field :description
    = f.submit 'Create'